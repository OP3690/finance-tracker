{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "ignoreCommand": "find . -type f -name '*.ts' -not -path './app/*' -not -path './components/*' -not -path './lib/*' -not -path './prisma/schema.prisma' -delete"
} 